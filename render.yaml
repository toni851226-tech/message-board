services:
  - type: web
    name: message-board
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: APPS_SCRIPT_URL
        sync: false
      - key: RECIPIENT_EMAIL
        sync: false
